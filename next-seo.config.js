const title = 'Random Shop';
const description = 'Random Shop';

const NextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    site_name: title,
  },
};

export default NextSeoConfig;